<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- TEACHER LIST VIEW -->
    <record id="view_teacher_list" model="ir.ui.view">
        <field name="name">school.teacher.list</field>
        <field name="model">school.teacher</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="phone"/>
                <field name="email"/>
                <field name="salary"/>
            </list>
        </field>
    </record>

    <!-- TEACHER FORM VIEW -->
    <record id="view_teacher_form" model="ir.ui.view">
        <field name="name">school.teacher.form</field>
        <field name="model">school.teacher</field>
        <field name="arch" type="xml">
            <form string="Teacher">

                <header>
                    <button name="%(school_management_system.action_teacher_pdf)d"
                            string="Print PDF"
                            type="action"
                            class="btn-primary"/>
                </header>

                <sheet>
                    <group>
                        <group string="Basic Information">
                            <field name="name"/>
                            <field name="phone"/>
                            <field name="email"/>
                        </group>

                        <group string="Professional Details">
                            <field name="subject_ids" widget="many2many_tags"/>
                            <field name="salary"/>
                            <field name="currency_id"/>
                        </group>
                    </group>

                    <!-- 🔐 BANK DETAILS (ADMIN ONLY) -->
                    <group string="Bank Details"
                           groups="school_management_system.group_school_admin">
                        <field name="bank_name"/>
                        <field name="bank_account_no"/>
                        <field name="bank_ifsc"/>
                    </group>

                </sheet>

            </form>
        </field>
    </record>

    <!-- TEACHER ACTION -->
    <record id="action_teacher" model="ir.actions.act_window">
        <field name="name">Teachers</field>
        <field name="res_model">school.teacher</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
